# Review — 0.6.0-rc.3

2026-09-10。実装担当によるレビュー。独立した別担当者によるレビューは行っていない。

## 変更と判断

ユーザーの明示依頼によりv0.5の固定配置を変更。生成り色のホーム、深緑の卓、点数、拡大できる手牌・河、画面中央の鳴き表示を実装した。鳴き元、点数増減、支援、2タップ打牌は維持。小画面の終盤で見つかった河・点数・副露の重なりを修正し、実際に生成した局面で確認した。

主対象はユーザー指定のiPhone。短い表示領域でも手牌と操作ボタンが収まるよう調整した。自分の鳴き・カン選択だけ背景のぼかしを外し、他のダイアログのぼかしは保持した。

RoundState / MatchStateがゲーム状態の本体で、UIは合法操作を送る。正式計算はセッションごとのmajiang-coreアダプターを使い、対局ループはプロジェクト側で管理する。CPUは自身の手牌と公開情報から判断。強いCPUには現物による守備と、役を見込める鳴きへの制限を加えた。

IndexedDBの書き込み完了後に次のCPU表示を予約する。入力待ちの不変状態では再書き込みしない。失敗時は進行を停止し、再試行でメモリに残る操作を保存。不正な保存は起動前に検証し、無断削除しない。明示操作で原文を書き出し、同一トランザクションで退避してホームへ戻れる。旧ラウンドスキーマも移行する。

service workerはローカル素材をキャッシュし、旧版の画面が閉じるまで更新待機する。キャッシュはスコープ別。UIから完全性を問い合わせ、失敗時は再試行を示す。未接続だけを理由に準備済みキャッシュを失敗扱いにしない。

GitHub Pages向けに、親パスの別アプリのworkerを自分のものと誤認しないよう登録スコープを一致確認する。公開ワークフローは既定ブランチのみを対象にコア検証とproduction buildを行い、distだけをPagesへ配置する。再現用CLIはメモリ内のセッションを使い、ブラウザ保存を書き換えずに操作列を追試できる。

保存・消去・破損退避では、読み込んだ値と現在の値を同じIndexedDB書き込みトランザクション内で比較する。別画面で進んでいた場合は上書きを止め、手元の局面の書き出しと最新の保存の読み込みを示す。最新の読み込みに失敗しても、古い局面が新しい保存を上書きできる状態にはしない。

React等のMIT表示と牌素材のCC0宣言を配布物に含める。CDN・配信フォント・通信対戦・アカウント・解析通信はない。

## 残る確認事項

- Windows環境ではSafari実機を検証できていない。WebKitは補助的な互換性検証であり、OSによる中断・ホーム画面起動・端末性能は実機で確認する。
- 保存検証は型・構造と主要な参照を対象とし、あらゆる改変について136枚の物理的整合性を証明するものではない。
- サイトデータ自体が消去された場合の外部バックアップはない。保存同期・アカウントは設けていない。
- Android実機のインストール・中程度性能端末の評価は未実施。

以上を残すリリース候補で、最終製品の受け入れ完了とはしていない。検証範囲はTEST_REPORT.mdに記録した。
